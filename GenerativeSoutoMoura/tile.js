 // Tile class
  class Tile {
    constructor(obj, x_size, y_size, z_size) {
      // Object
      this.obj = obj;
      // Edges Size
      this.x_size = x_size;
      this.y_size = y_size;
      this.z_size = z_size;
    }
  }
  